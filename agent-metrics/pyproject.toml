[project]
name = "agent-metrics"
version = "0.1.0"
description = "Metrics analysis agent specialized in querying Mimir via MCP"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
    "common",
]
