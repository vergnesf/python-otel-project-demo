[project]
name = "common-models"
version = "0.1.0"
description = "Shared business models for microservices"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.9.2",
]

[dependency-groups]
dev = [
    "ruff>=0.8.4",
    "black>=24.10.0",
    "pyright>=1.1.390",
]
