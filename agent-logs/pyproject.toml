[project]
name = "agent-logs"
version = "0.1.0"
description = "Logs analysis agent specialized in querying Loki via MCP"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
    "common",
]
