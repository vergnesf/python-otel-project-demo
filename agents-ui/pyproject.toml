[project]
name = "agents-ui"
version = "0.1.0"
description = "Reflex-based web UI for the observability agentic network"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "reflex>=0.6.0",
    "httpx>=0.27.0",
    "pydantic>=2.9.2",
]
